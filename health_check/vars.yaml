NAMESPACE: ${NAMESPACE}
APP_SOCKET: ${APP_SOCKET}
NODE_PORT: ${NODE_PORT}
ENVIRONMENT: ${ENVIRONMENT}
PANOPTICA_TOKEN: ${PANOPTICA_TOKEN}
PANOPTICA_CLUSTER_ID: ${PANOPTICA_CLUSTER_ID}
flows: []
result: ""
status: "Success"